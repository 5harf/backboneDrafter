var Player = Backbone.Model.extend({
  initialize: function() {
  // this.set('deck', new Deck);
  this.set('pack', new Pack);
  this.set('deck', new Deck);  
  // this.set('packs', new Packs);

  // var pack = this.get('firstPack');
  // this.get('packs').add(pack);
  }, 



})
